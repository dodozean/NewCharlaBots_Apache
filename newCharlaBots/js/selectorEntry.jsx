import React from "react";
import ReactDOM from "react-dom";
import Selector from "./selector";

// Render the LanguageSelector component into the root element
ReactDOM.render(
  <Selector />,
  document.getElementById("reactEntry")
);
